/**
 * Routes barrel export
 * All API routes for the OpenStream server
 */

export { default as authRoutes } from './auth';
export { default as usersRoutes } from './users';
export { default as settingsRoutes } from './settings';
